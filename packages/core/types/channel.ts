export type FlarieChannel = {
  /**
   * The id of the channel this interaction was sent in
   */
  id: string;

  /**
   * The name of the channel this interaction was sent in
   */
  name: string;
};
